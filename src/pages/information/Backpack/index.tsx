import React from "react";
import CommonTabs from "../CommonTabs";

interface Props {}

function Backpack(props: Props) {
  const {} = props;

  return <CommonTabs tabItems={[]} DetailComp={() => null} />;
}

export default Backpack;
